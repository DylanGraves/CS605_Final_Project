package query.analyzer;

// This class exists as a workaround, because JavaFX doesn't like to be deployed as a jar executable.
public class Launcher {
    public static void main(String[] args) {
        Program.main(args);
    }
}
